import { StyleSheet } from 'react-native';
import { Colors } from '../../styles';

export const tintColor = Colors.primary;
export const titleTintColor = Colors.white;
export const leftTintColor = Colors.white;
export const rightTintColor = Colors.white;

export default StyleSheet.create({

});
